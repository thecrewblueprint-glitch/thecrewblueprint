(function(){'use strict';const slug=document.body.dataset.courseSlug;const course=window.CBPFieldExpansionCourses&&window.CBPFieldExpansionCourses[slug];if(!course){document.getElementById('lessonContent').innerHTML='<div class="stop-box"><h3>Course data unavailable</h3><p>This staging route could not load its course record.</p></div>';return;}const data=document.getElementById('courseData');data.textContent=JSON.stringify(course).replaceAll('<','\\u003c');const title=document.getElementById('topTitle');if(title)title.textContent=`${course.tier} · ${course.title}`;})();
